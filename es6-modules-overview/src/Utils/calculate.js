const add = ( first , second) =>{
    return first + second;
}

const multipy = (first, second) =>{
    return first * second;
}

const divideNumber = (first, second) =>{
    return first / second;
}

export { add , multipy, divideNumber}
// export default add ;
// const calculator = { add: add }
// export default calculator;
